# Simple workflow for deploying static content <!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>For My Favorite Person</title>
    <style>
        :root {
            --primary-pink: #ffafbd;
            --secondary-pink: #ffc3a0;
            --text-color: #4a4a4a;
        }

        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, var(--primary-pink), var(--secondary-pink));
            color: var(--text-color);
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        header {
            text-align: center;
            padding: 50px 20px;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 10px;
            color: white;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        .container {
            background: rgba(255, 255, 255, 0.9);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            max-width: 600px;
            width: 90%;
            text-align: center;
            margin-bottom: 50px;
        }
           .photo-placeholder {
            width: 100%;
            height: 300px;
            background: #eee;
            border-radius: 15px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .photo-placeholder img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .message {
            line-height: 1.6;
            font-size: 1.1rem;
        }

        .heart {
            color: #e74c3c;
            font-size: 1.5rem;
            animation: beat 1s infinite alternate;
        }

        @keyframes beat {
            to { transform: scale(1.2); }
        }

        .footer {
            padding: 20px;
            font-size: 3rem;
            color: white;
        }
    </style>
</head>
<body>

    <header>
        <h1>Hiiiiiiiii [ Angle ] ❤️</h1>
        <p>Just a little something to remind you how much you mean to me.</p>
    </header>

    <div class="container">
<h2>''Merry Christmas, my love! You're the best gift ever''</h2>       
        <div class="message">
            <p>Every time with you is my favorite lucky. I built this page just for you because a regular card wasn't enough to say everything I feel🤗❤.</p>
            <p>"Missing you loads this Christmas, but my love for you shines brighter than any lights''. Merry Christmas, My ''Angel''.</p> 
            <p>Thank you for being my best friend, my partner, and my person.</p>
            <div class="heart">❤️</div>
        </div>
    </div>

    <div class="footer">
        Made with love by [ Your = Bachaa ] | 
    </div>

</body>
</html>
to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
